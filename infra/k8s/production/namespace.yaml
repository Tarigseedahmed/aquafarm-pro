apiVersion: v1
kind: Namespace
metadata:
  name: aquafarm-prod
  labels:
    name: aquafarm-prod
    environment: production
    app: aquafarm
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: aquafarm-quota
  namespace: aquafarm-prod
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.cpu: "8"
    limits.memory: 16Gi
    persistentvolumeclaims: "10"
    services: "10"
    secrets: "20"
    configmaps: "20"
---
apiVersion: v1
kind: LimitRange
metadata:
  name: aquafarm-limits
  namespace: aquafarm-prod
spec:
  limits:
  - default:
      cpu: "1"
      memory: "2Gi"
    defaultRequest:
      cpu: "100m"
      memory: "256Mi"
    type: Container
