# ==============================
# AquaFarm Pro - Production-lite
# Docker Compose environment
# Rename/copy to .env and adjust values
# ==============================

# General
NODE_ENV=production
PORT=3000

# Domain / Networking
DOMAIN=aquafarm.cloud
VPS_HOST=srv1029413.hstgr.cloud
CORS_ORIGIN=https://aquafarm.cloud
NEXT_PUBLIC_API_URL=https://api.aquafarm.cloud
NEXT_PUBLIC_APP_URL=https://aquafarm.cloud

# JWT / Auth
# Generate a strong secret: openssl rand -base64 48
JWT_SECRET=CHANGE_ME_TO_A_STRONG_SECRET

# Database (PostgreSQL)
DB_TYPE=postgres
# Option A: discrete fields
DB_NAME=aquapro
DB_USER=postgres
DB_PASSWORD=CHANGE_ME_DB_PASSWORD
# Option B: single URL (overrides discrete fields if provided)
# DATABASE_URL=postgres://postgres:CHANGE_ME_DB_PASSWORD@postgres:5432/aquapro

# Redis (optional password; leave empty if not used)
REDIS_PASSWORD=

# Optional Integrations
HOSTINGER_API_KEY=

# Monitoring (Prometheus/Grafana via compose profile `monitoring`)
# No secrets required by default; restrict /_metrics via Nginx ACLs


