formats:
  - oas3

rules:
  info-title-required:
    description: Ensure API has a title
    message: info.title must be present
    severity: error
    given: $.info
    then:
      field: title
      function: truthy
