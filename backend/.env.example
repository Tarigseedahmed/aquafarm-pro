## =====================================
## AquaFarm Backend Environment Example
## =====================================

# --- General ---
NODE_ENV=development
PORT=3000
LOG_LEVEL=debug

# --- Auth / JWT ---
JWT_SECRET=change_me
JWT_EXPIRES_IN=900s
REFRESH_EXPIRES_IN=7d

# --- Multi-Tenancy Defaults ---
DEFAULT_TENANT_CODE=default
DEFAULT_TENANT_NAME=Default Tenant

# --- Database Selection ---
# Choose ONE of the following approaches.

# 1) SQLite quick start (local development with zero setup)
# DB_TYPE=sqlite
# SQLITE_DB=aquafarm.db

# 2) PostgreSQL discrete parameters
DB_TYPE=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=aquafarm
DB_USER=postgres
DB_PASSWORD=postgres
DB_SSL=false

# 3) PostgreSQL single DATABASE_URL (overrides discrete params if set)
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/aquafarm

# --- Internationalization ---
APP_DEFAULT_LOCALE=ar
APP_REQUIRE_RTL=true

# --- Optional Integrations (uncomment when used) ---
# Redis (used for pub/sub notifications + future caching). If absent, app gracefully degrades to in-process only.
# REDIS_URL=redis://localhost:6379
# SENTRY_DSN=
# FEATURE_FLAG_X=false